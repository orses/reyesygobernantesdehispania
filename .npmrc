# Pina versiones exactas al instalar nuevas dependencias (sin ^ ni ~)
save-exact=true

# Ejecuta npm audit automáticamente en cada install
audit=true

# Oculta el mensaje de financiación (no relevante para seguridad)
fund=false
